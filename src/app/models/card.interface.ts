export interface CardI {
    cardNumber: string,
    cardType: string,
    cardholderName: string,
    cvv: string,
    expiryMonth: number,
    expiryYear: number,
    postalCode: string,
    redactedCardNumber: string
}